import React from 'react'

const OneHwUserInfo = (props) => {
  return (
    <div>
      <h1>{props.name} is {props.age}  old and lives in {props.city}  </h1>
      
    </div>
  );
};

export default OneHwUserInfo;
